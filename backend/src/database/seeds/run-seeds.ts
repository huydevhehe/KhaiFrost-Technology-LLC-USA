// Stub: the seeding module fills this in (bootstrap owner, default settings, UI translations)
async function main(): Promise<void> {
  console.log('No seeds registered yet.');
}

main().catch((error: unknown) => {
  console.error(error);
  process.exit(1);
});
