export enum NavigationLinkType {
  PAGE = 'page',
  EXTERNAL = 'external',
  PATH = 'path',
  // A label without a link, used for dropdown headings and footer column titles
  NONE = 'none',
}

export const MAX_NAVIGATION_DEPTH = 3;
export const MAX_NAVIGATION_ITEMS = 100;
